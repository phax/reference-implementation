spring:
  application:
    name: efti-gate
  datasource:
    url: jdbc:postgresql://localhost:5432/efti
    username: ingroup
    password: root
    driverClassName: org.postgresql.Driver

server:
  port: 9000
